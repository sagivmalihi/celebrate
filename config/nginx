server 
{
    listen 8080 default_server;
    listen [::]:8080 default_server ipv6only=on;

    location / { try_files $uri @celebrate; }

    location @celebrate
    {
      include uwsgi_params;
      uwsgi_pass unix:/tmp/celebrate-uwsgi.sock;
    }
}

